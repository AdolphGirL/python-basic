1. 請使用迴圈敘述，撰寫一程式，讓使用者輸入兩個正整數a、b(a < b)，
   利用迴圈計算，從a開始的偶數加到b的總和。例如，輸入 a=1、b=100，則輸出結果為2550

2. 撰寫一程式，讓使用者輸入數字，輸入的動作直到輸入值為9999才結束，然後找出最小值，並輸出最小值
3. 撰寫一程式，將使用者輸入的兩個數字作為參數傳遞給一個名為compute(x, y)的函數，此函數將回傳x和y的乘績
4. 請使用選擇敘述撰寫一程式，讓使用者輸入一個西元年份，然後判斷它是否為閏年（leap year）或平年。
   其判斷規則為：每四年一閏，每百年不閏，但每四百年也一閏。
5. 請撰寫一程式，要求使用者輸入十個數字並存放在串列中。
   接著由大到小的順序顯示最大的 3 個數字。(還沒教list，先用變數儲存)
6. 請撰寫一程式，讓使用者輸入五個數字，計算並輸出這五個數字之數值、總和及平均數
7. 自訂函式hi，印出「hello world」到螢幕上。
8. 自訂函式min，輸入兩個參數a與b，將較小的數字回傳回來。
9. 函式area的定義，輸入參數x與y，回傳x乘以y的結果。
   請輸入長度、請輸入寬度
10. 函式與變數的作用範圍
    g = 5
    def f1():
        print(g)
    f1()

    g = 5
    def f2():
        #print(g)
        g = 10
        print(g)
    f2()
    print(g)

    g = 5
    def f():
        global g
        print(g)
        g = 10
        print(g)
    f()
    print(g)
11. 我們利用自訂函式BMI，輸入體重與身高回傳BMI值，利用BMI值與「BMI與肥胖等級標準」表，
    使用選擇結構顯示BMI值與對應的肥胖等級。[身高(M)]
    def BMI(w, h):
        return w/(h*h)
    
    if (bmi < 18):
        print('體重過輕')
    elif (bmi < 24):
        print('體重正常')
    elif (bmi < 27):
        print('體重過重')
    else:
        print('體重肥胖')
    